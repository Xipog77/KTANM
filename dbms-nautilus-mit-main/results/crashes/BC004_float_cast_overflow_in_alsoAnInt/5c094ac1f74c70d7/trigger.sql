CREATE TABLE IF NOT EXISTS p(c1 INTEGER);
CREATE TABLE IF NOT EXISTS q(c2 BOOLEAN PRIMARY KEY);
INSERT INTO q (rowid) VALUES (random() COLLATE BINARY || TRUE || TRUE || TRUE) ON CONFLICT(c2) DO UPDATE SET rowid = CURRENT_TIMESTAMP;
EXPLAIN QUERY PLAN VALUES (FALSE);