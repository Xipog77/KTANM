CREATE TABLE IF NOT EXISTS p(a INTEGER NOT NULL DEFAULT CURRENT_DATE, c2 BLOB, b NUMERIC GENERATED ALWAYS AS (hex(hex(hex(a)))) STORED);
CREATE INDEX IF NOT EXISTS idx1 ON p(c2);
INSERT INTO p (_rowid_) VALUES (FALSE);
PRAGMA integrity_check;
SELECT hex(zeroblob(0));