CREATE TABLE IF NOT EXISTS p(c3 BLOB NOT NULL);
CREATE TABLE IF NOT EXISTS q(c3 FLOAT NOT NULL);
INSERT OR ROLLBACK INTO q VALUES (hex(hex(hex(hex(hex(CURRENT_DATE << CURRENT_DATE))))));
PRAGMA quick_check;