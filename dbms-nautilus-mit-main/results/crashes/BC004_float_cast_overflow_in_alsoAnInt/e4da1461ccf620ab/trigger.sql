CREATE TABLE IF NOT EXISTS p(c3 INT UNIQUE);
CREATE TABLE IF NOT EXISTS q(rowid FLOAT CHECK (CURRENT_TIMESTAMP BETWEEN FALSE AND FALSE));
INSERT OR ROLLBACK INTO q VALUES (hex(hex(CURRENT_DATE)));
PRAGMA quick_check;
SELECT hex(zeroblob(-1));