CREATE TABLE IF NOT EXISTS t3 (c1 REAL CHECK (EXISTS (WITH RECURSIVE t1 (_rowid_) AS (VALUES (CURRENT_TIME)) VALUES (TRUE) UNION SELECT * FROM t2 GROUP BY CURRENT_DATE WINDOW w1 AS () ORDER BY FALSE COLLATE BINARY NULLS LAST)), _rowid_ BLOB COLLATE NOCASE) STRICT;
DROP VIEW IF EXISTS v1;