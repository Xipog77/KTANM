CREATE TABLE IF NOT EXISTS p(a INT NOT NULL);
CREATE VIEW IF NOT EXISTS v1 AS VALUES (FALSE);
VALUES (count(*) FILTER (WHERE CURRENT_TIME) OVER (PARTITION BY hex(hex(hex(CAST(hex(hex(CURRENT_TIME)) AS BOOLEAN)))), CURRENT_TIME), CURRENT_DATE);