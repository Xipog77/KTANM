CREATE TABLE IF NOT EXISTS p(a REAL);
SELECT * FROM p JOIN p k_910_300__ixx64_0yer_od__05 ON FALSE IN (VALUES (count(*) OVER (PARTITION BY (SELECT DISTINCT * FROM p ORDER BY CURRENT_DATE DESC NULLS LAST LIMIT (VALUES (CURRENT_TIME) EXCEPT SELECT CURRENT_DATE ORDER BY CURRENT_TIME COLLATE NOCASE DESC NULLS LAST)) ORDER BY CURRENT_TIME DESC GROUPS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW)));
SELECT hex(zeroblob(9223372036854775807));