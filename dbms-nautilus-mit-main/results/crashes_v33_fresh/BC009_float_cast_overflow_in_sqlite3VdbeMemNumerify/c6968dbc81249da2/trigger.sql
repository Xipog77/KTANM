CREATE TABLE IF NOT EXISTS p(a, b NOT NULL GENERATED ALWAYS AS (a IS NULL));
INSERT INTO p(a) VALUES(0);
PRAGMA integrity_check;
SELECT group_concat(CURRENT_TIMESTAMP, ' 9') FROM p GROUP BY CAST(hex(hex(hex(hex(hex(hex(CURRENT_TIMESTAMP)))))) AS BOOLEAN);