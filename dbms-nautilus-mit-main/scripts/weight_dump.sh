#!/usr/bin/env bash
# weight_dump.sh — Stub for grammar weight observability (optional)
# Creates a weight dump file in the workdir for post-campaign analysis.
# If this fails, the campaign still runs fine.
echo "weight_dump: skipped (stub)"
